<?xml version="1.0" encoding="utf-8"?>
<!--
    item_med_card.xml
    Place in: res/layout/item_med_card.xml
    Inflated by MainActivity for each schedule fetched from Firestore.
    Clicking the card opens LogDoseActivity.
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:layout_marginBottom="10dp"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="#FFFFFF"
    app:cardCornerRadius="16dp"
    app:cardElevation="1dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:gravity="center_vertical">

        <!-- Pill icon -->
        <ImageView
            android:id="@+id/iv_pill_icon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_pill"
            android:contentDescription="Pill" />

        <!-- Name + time/food info -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@id/iv_pill_icon"
            android:layout_marginStart="14dp"
            android:orientation="vertical">

            <!-- tv_med_name: set in MainActivity -->
            <TextView
                android:id="@+id/tv_med_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Medication"
                android:textColor="#1A1A1A"
                android:textSize="15sp"
                android:textStyle="bold" />

            <!-- tv_med_info: set to "HH:MM · Food option" in MainActivity -->
            <TextView
                android:id="@+id/tv_med_info"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="10:00 AM · After Food"
                android:textColor="#999999"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Chevron arrow -->
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_chevron_right"
            android:contentDescription="Open" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>